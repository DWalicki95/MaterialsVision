# Pinned to the environment the fine-tuning experiment was verified
# in: Python 3.12.3, one virtualenv, every version below read back from
# it rather than chosen. Only packages the project imports directly are
# listed; anything reached through one of these resolves with it.

# Numerics, imaging and reporting used throughout the package.
GPUtil==1.4.0
matplotlib==3.10.8
mlflow==3.7.0
numpy==2.2.6
opencv-python-headless==4.12.0.88
pandas==2.3.3
Pillow==12.0.0
psutil==7.1.3
PyYAML==6.0.3
scikit-image==0.25.2
scipy==1.16.3
seaborn==0.13.2
tifffile==2025.12.12
tqdm==4.67.1

# Deep learning. The verified environment runs the CUDA 12.8 build,
# torch 2.9.1+cu128, installed from PyTorch's own index; the plain pin
# below resolves to whichever build PyPI serves for the platform.
torch==2.9.1
torchvision==0.24.1

# Segmentation model stacks. micro_sam below 1.8.12 imports vigra,
# which is not on PyPI and only installs through conda, so 1.8.12 is
# the lowest version this file can express. peft-sam is not published
# on PyPI at all and is pinned to the commit it was installed from,
# because its version string, 0.0.1, identifies nothing.
albumentations==2.0.8
cellpose==4.0.8
micro_sam==1.8.12
peft_sam @ git+https://github.com/computational-cell-analytics/peft-sam@2ec9874c515020f92a5bd5ee9a20312031b44a2f
torch_em==0.10.3

# Used only by code outside the segmentation and analysis pipeline:
# shapely by the synthetic microstructure generator, stardist by the
# exploratory notebooks. Neither is installed in the verified
# environment, so neither pin has been checked against a working
# install the way every pin above has.
Shapely==2.1.2
stardist==0.9.2

# Development tooling, for the checks the repository documents.
flake8==7.3.0
isort==5.12.0
mypy==2.3.0
pre-commit==4.6.1
pytest==9.1.1
